name: Full Flutter CI

# Disabled automatic push/pr triggers on fork to avoid consuming private Actions minutes across 15 platforms (including macOS and Windows).
# Trigger manually via workflow_dispatch when needed.
on:
  workflow_dispatch:
#   pull_request:
#     paths-ignore:
#     - "docs/**"
#     - "README.md"
#   push:
#     branches:
#       - master
#     paths-ignore:
#       - ".github/**"
#       - "docs/**"
#       - "README.md"
#       - "res/**"
#       - "appimage/**"
#       - "flatpak/**"

jobs:
  run-ci:
    uses: ./.github/workflows/flutter-build.yml
    with:
      upload-artifact: false
